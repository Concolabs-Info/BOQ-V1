# Shared enums

Backend extraction enums are defined once in `backend/app/modules/pre/schemas.py`. Frontend option lists mirror those closed values. When a new value is added, update the backend schema, OpenAPI contract and frontend list in the same change.
