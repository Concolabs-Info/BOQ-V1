# End-to-end tests

Add browser/API tests here as real representative drawing fixtures are approved. The required acceptance flow is upload → triage/edit → confirm sheets/viewports/storeys → scale → height → specifications → freeze → verify writes are rejected.
