# Embedded Beam Engine

This directory is the production integration of the supplied standalone Beams project.
The S0-S6 extraction, geometry, vision, dimension, junction and workbook logic is kept
as the engine core. Quanto supplies project files, runs the old gates automatically,
and adapts the final result to the Quanto canvas/Review/BOQ UI.
